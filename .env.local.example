# Secrets only. Copy to .env.local (gitignored).
# Without a real key the app runs in "sample mode": generation returns
# hand-written placeholder content instead of calling Claude.
ANTHROPIC_API_KEY=your-api-key-here

# Non-secret config (DATABASE_URL) lives in .env, which is committed.
